---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: rpcproviders.kontractdeployer.expedio.xyz
spec:
  group: kontractdeployer.expedio.xyz
  names:
    kind: RPCProvider
    listKind: RPCProviderList
    plural: rpcproviders
    singular: rpcprovider
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RPCProvider is the Schema for the rpcproviders API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RPCProviderSpec defines the desired state of RPCProvider
            properties:
              providerName:
                description: ProviderName is the name of the RPC provider (e.g., Infura)
                type: string
              secretRef:
                description: SecretRef references a Kubernetes Secret that contains
                  the API token and endpoint
                properties:
                  apiEndpoint:
                    description: APIEndpoint is the key within the secret that contains
                      the API endpoint
                    type: string
                  apiKey:
                    description: APIKey is the key within the secret that contains
                      the API token
                    type: string
                  name:
                    description: Name of the secret in the same namespace
                    type: string
                required:
                - apiEndpoint
                - apiKey
                - name
                type: object
              timeout:
                description: Timeout defines the request timeout for the RPC calls
                type: string
            required:
            - providerName
            - secretRef
            - timeout
            type: object
          status:
            description: RPCProviderStatus defines the observed state of RPCProvider
            properties:
              apiEndpoint:
                description: APIEndpoint is the actual API endpoint used for RPC calls
                type: string
              status:
                description: Status indicates the current status of the RPCProvider
                type: string
            required:
            - apiEndpoint
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
